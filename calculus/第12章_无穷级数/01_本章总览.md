# 第12章 无穷级数

## 本章总览

- 如果面对的是一串数字相加，先判断这个级数有没有和。
- 如果面对的是一个函数，先看它能不能写成幂级数。
- 如果面对的是周期函数，先看它能不能写成傅里叶级数。
- `u_n \to 0` 只是级数收敛的必要条件，不是充分条件。
- 幂级数在端点处必须单独检查，不能只看收敛半径。
- 傅里叶级数在间断点处，一般收敛到左右极限的平均值，不一定收敛到函数原值。
