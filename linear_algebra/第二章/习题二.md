# 第二章 矩阵及其运算 · 习题二

## 题 5

**(2)** 若 $\boldsymbol{A}^2 = \boldsymbol{A}$，则 $\boldsymbol{A} = \boldsymbol{O}$ 或 $\boldsymbol{A} = \boldsymbol{E}$；

**(3)** 若 $\boldsymbol{A}\boldsymbol{X} = \boldsymbol{A}\boldsymbol{Y}$，且 $\boldsymbol{A} \neq \boldsymbol{O}$，则 $\boldsymbol{X} = \boldsymbol{Y}$。

## 【作业】题 6

**(1)** 设 $\boldsymbol{A} = \begin{pmatrix} 1 & 0 \\ \lambda & 1 \end{pmatrix}$，求 $\boldsymbol{A}^2, \boldsymbol{A}^3, \dots, \boldsymbol{A}^k$；

**(2)** 设 $\boldsymbol{A} = \begin{pmatrix} \lambda & 1 & 0 \\ 0 & \lambda & 1 \\ 0 & 0 & \lambda \end{pmatrix}$，求 $\boldsymbol{A}^4$。

## 【作业】题 7

**(1)** 设 $\boldsymbol{A} = \begin{pmatrix} 3 & 1 \\ 1 & -3 \end{pmatrix}$，求 $\boldsymbol{A}^{50}$ 和 $\boldsymbol{A}^{51}$；

**(2)** 设 $\boldsymbol{a} = \begin{pmatrix} 2 \\ 1 \\ -3 \end{pmatrix}$，$\boldsymbol{b} = \begin{pmatrix} 1 \\ 2 \\ 4 \end{pmatrix}$，$\boldsymbol{A} = \boldsymbol{a}\boldsymbol{b}^{\mathrm{T}}$，求 $\boldsymbol{A}^{100}$。

## 【作业】题 8

**(1)** 设 $\boldsymbol{A}, \boldsymbol{B}$ 为 $n$ 阶矩阵，且 $\boldsymbol{A}$ 为对称矩阵，证明 $\boldsymbol{B}^{\mathrm{T}}\boldsymbol{A}\boldsymbol{B}$ 也是对称矩阵；

**(2)** 设 $\boldsymbol{A}, \boldsymbol{B}$ 都是 $n$ 阶对称矩阵，证明 $\boldsymbol{A}\boldsymbol{B}$ 是对称矩阵的充分必要条件是 $\boldsymbol{A}\boldsymbol{B} = \boldsymbol{B}\boldsymbol{A}$；

**(3)** 设 $\boldsymbol{A}$ 为 $n$ 阶方阵，若满足 $\boldsymbol{A}^{\mathrm{T}} = -\boldsymbol{A}$，即 $a_{ij} = -a_{ji}\ (i, j = 1, 2, \dots, n)$，则称 $\boldsymbol{A}$ 为反对称矩阵（亦称为反称矩阵）。设 $\boldsymbol{A}, \boldsymbol{B}$ 都是 $n$ 阶反对称矩阵，证明 $\boldsymbol{A}\boldsymbol{B}$ 是反对称矩阵的充分必要条件是 $\boldsymbol{A}\boldsymbol{B} = -\boldsymbol{B}\boldsymbol{A}$。

## 【作业】题 9

求下列矩阵的逆矩阵：

**(1)** $\begin{pmatrix} 1 & 2 \\ 2 & 5 \end{pmatrix}$；

**(2)** $\begin{pmatrix} \cos \theta & -\sin \theta \\ \sin \theta & \cos \theta \end{pmatrix}$；

**(3)** $\begin{pmatrix} 1 & 2 & -1 \\ 3 & 4 & -2 \\ 5 & -4 & 1 \end{pmatrix}$；

**(4)** $\begin{pmatrix} a_1 & & & 0 \\ & a_2 & & \\ & & \ddots & \\ 0 & & & a_n \end{pmatrix}\ (a_1 a_2 \cdots a_n \neq 0)$。

## 题 10

已知线性变换
$$
\begin{cases}
x_1 = 2y_1 + 2y_2 + y_3, \\
x_2 = 3y_1 + y_2 + 5y_3, \\
x_3 = 3y_1 + 2y_2 + 3y_3,
\end{cases}
$$
求从变量 $x_1, x_2, x_3$ 到变量 $y_1, y_2, y_3$ 的线性变换。

## 题 11

设 $\boldsymbol{J}$ 是元素全为 $1$ 的 $n$ ($n \ge 2$) 阶方阵。证明 $\boldsymbol{E} - \boldsymbol{J}$ 是可逆方阵，且 $(\boldsymbol{E} - \boldsymbol{J})^{-1} = \boldsymbol{E} - \frac{1}{n-1}\boldsymbol{J}$，这里 $\boldsymbol{E}$ 是与 $\boldsymbol{J}$ 同阶的单位矩阵。

## 【作业】题 12

设 $\boldsymbol{A}^k = \boldsymbol{O}$ ($k$ 为正整数)，证明
$$
(\boldsymbol{E} - \boldsymbol{A})^{-1} = \boldsymbol{E} + \boldsymbol{A} + \boldsymbol{A}^2 + \dots + \boldsymbol{A}^{k-1}.
$$

## 【作业】题 13

设方阵 $\boldsymbol{A}$ 满足 $\boldsymbol{A}^2 - \boldsymbol{A} - 2\boldsymbol{E} = \boldsymbol{O}$，证明 $\boldsymbol{A}$ 及 $\boldsymbol{A} + 2\boldsymbol{E}$ 都可逆，并求 $\boldsymbol{A}^{-1}$ 及 $(\boldsymbol{A} + 2\boldsymbol{E})^{-1}$。

## 【作业】题 14

解下列矩阵方程：

**(1)** $\begin{pmatrix} 2 & 5 \\ 1 & 3 \end{pmatrix} \boldsymbol{X} = \begin{pmatrix} 4 & -6 \\ 2 & 1 \end{pmatrix}$；

**(2)** $\boldsymbol{X} \begin{pmatrix} 2 & 1 & -1 \\ 2 & 1 & 0 \\ 1 & -1 & 1 \end{pmatrix} = \begin{pmatrix} 1 & -1 & 3 \\ 4 & 3 & 2 \end{pmatrix}$；

**(3)** $\begin{pmatrix} 1 & 4 \\ -1 & 2 \end{pmatrix} \boldsymbol{X} \begin{pmatrix} 2 & 0 \\ -1 & 1 \end{pmatrix} = \begin{pmatrix} 3 & 1 \\ 0 & -1 \end{pmatrix}$；

**(4)** $\boldsymbol{A}\boldsymbol{X}\boldsymbol{B} = \boldsymbol{C}$，其中 $\boldsymbol{A} = \begin{pmatrix} 2 & 1 \\ 5 & 4 \end{pmatrix}$，$\boldsymbol{B} = \begin{pmatrix} 1 & 3 & 3 \\ 1 & 4 & 3 \\ 1 & 3 & 4 \end{pmatrix}$，$\boldsymbol{C} = \begin{pmatrix} 1 & 0 & -1 \\ 1 & -2 & 0 \end{pmatrix}$。

## 【作业】题 15

分别应用克拉默法则和逆矩阵解下列线性方程组：

**(1)**
$$
\begin{cases}
x_1 + 2x_2 + 3x_3 = 1, \\
2x_1 + 2x_2 + 5x_3 = 2, \\
3x_1 + 5x_2 + x_3 = 3;
\end{cases}
$$

**(2)**
$$
\begin{cases}
x_1 + x_2 + x_3 = 2, \\
x_1 + 2x_2 + 4x_3 = 3, \\
x_1 + 3x_2 + 9x_3 = 5.
\end{cases}
$$

## 【作业】题 16

设 $\boldsymbol{A}$ 为 $3$ 阶矩阵，$|\boldsymbol{A}| = \frac{1}{2}$，求 $|(2\boldsymbol{A})^{-1} - 5\boldsymbol{A}^*|$。

## 题 17

设 $\boldsymbol{A} = \begin{pmatrix} 0 & 3 & 3 \\ 1 & 1 & 0 \\ -1 & 2 & 3 \end{pmatrix}$，$\boldsymbol{A}\boldsymbol{B} = \boldsymbol{A} + 2\boldsymbol{B}$，求 $\boldsymbol{B}$。

## 题 18

设 $\boldsymbol{A} = \begin{pmatrix} 1 & 0 & 1 \\ 0 & 2 & 0 \\ 1 & 0 & 1 \end{pmatrix}$，且 $\boldsymbol{A}\boldsymbol{B} + \boldsymbol{E} = \boldsymbol{A}^2 + \boldsymbol{B}$，求 $\boldsymbol{B}$。

## 【作业】题 19

设 $\boldsymbol{A} = \mathrm{diag}(1, -2, 1)$，$\boldsymbol{A}^*\boldsymbol{B}\boldsymbol{A} = 2\boldsymbol{B}\boldsymbol{A} - 8\boldsymbol{E}$，求 $\boldsymbol{B}$。

## 【作业】题 20

已知矩阵 $\boldsymbol{A}$ 的伴随矩阵 $\boldsymbol{A}^* = \mathrm{diag}(1, 1, 1, 8)$，且 $\boldsymbol{A}\boldsymbol{B}\boldsymbol{A}^{-1} = \boldsymbol{B}\boldsymbol{A}^{-1} + 3\boldsymbol{E}$，求 $\boldsymbol{B}$。

## 题 21

设 $\boldsymbol{P}^{-1}\boldsymbol{A}\boldsymbol{P} = \boldsymbol{\Lambda}$，其中 $\boldsymbol{P} = \begin{pmatrix} -1 & -4 \\ 1 & 1 \end{pmatrix}$，$\boldsymbol{\Lambda} = \begin{pmatrix} -1 & 0 \\ 0 & 2 \end{pmatrix}$，求 $\boldsymbol{A}^{11}$。

## 题 22

设 $\boldsymbol{A}\boldsymbol{P} = \boldsymbol{P}\boldsymbol{\Lambda}$，其中 $\boldsymbol{P} = \begin{pmatrix} 1 & 1 & 1 \\ 1 & 0 & -2 \\ 1 & -1 & 1 \end{pmatrix}$，$\boldsymbol{\Lambda} = \begin{pmatrix} -1 & & \\ & 1 & \\ & & 5 \end{pmatrix}$，求 $\varphi(\boldsymbol{A}) = \boldsymbol{A}^8 (5\boldsymbol{E} - 6\boldsymbol{A} + \boldsymbol{A}^2)$。

## 题 23

设矩阵 $\boldsymbol{A}$ 可逆，证明其伴随矩阵 $\boldsymbol{A}^*$ 也可逆，且 $(\boldsymbol{A}^*)^{-1} = (\boldsymbol{A}^{-1})^*$。

## 题 24

设 $n$ 阶矩阵 $\boldsymbol{A}$ 的伴随矩阵为 $\boldsymbol{A}^*$，证明：

**(1)** 若 $|\boldsymbol{A}| = 0$，则 $|\boldsymbol{A}^*| = 0$；

**(2)** $|\boldsymbol{A}^*| = |\boldsymbol{A}|^{n-1}$。

## 题 25

计算如下矩阵乘积：
$$
\begin{pmatrix} 1 & 2 & 1 & 0 \\ 0 & 1 & 0 & 1 \\ 0 & 0 & 2 & 1 \\ 0 & 0 & 0 & 3 \end{pmatrix}
\begin{pmatrix} 1 & 0 & 3 & 1 \\ 0 & 1 & 2 & -1 \\ 0 & 0 & -2 & 3 \\ 0 & 0 & 0 & -3 \end{pmatrix}.
$$

## 题 26

设 $\boldsymbol{A} = \begin{pmatrix} 3 & 4 & 0 & 0 \\ 4 & -3 & 0 & 0 \\ 0 & 0 & 2 & 0 \\ 0 & 0 & 2 & 2 \end{pmatrix}$，求 $|\boldsymbol{A}^8|$ 及 $\boldsymbol{A}^4$。

## 题 27

设 $n$ 阶矩阵 $\boldsymbol{A}$ 及 $s$ 阶矩阵 $\boldsymbol{B}$ 都可逆，求
$$
\begin{pmatrix} \boldsymbol{O} & \boldsymbol{A} \\ \boldsymbol{B} & \boldsymbol{O} \end{pmatrix}^{-1}.
$$

## 【作业】题 28

求下列矩阵的逆矩阵：

**(1)** $\begin{pmatrix} 5 & 2 & 0 & 0 \\ 2 & 1 & 0 & 0 \\ 0 & 0 & 8 & 3 \\ 0 & 0 & 5 & 2 \end{pmatrix}$；

**(2)** $\begin{pmatrix} 0 & 0 & \frac{1}{5} \\ 2 & 1 & 0 \\ 4 & 3 & 0 \end{pmatrix}$。
