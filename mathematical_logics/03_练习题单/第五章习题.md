# 第五章 一阶逻辑等值演算与推理 · 习题五

## 题 1

设个体域 $D = \{a, b, c\}$，在 $D$ 中消去公式 $\forall x (F(x) \land \exists y G(y))$ 的量词。甲、乙用了不同的演算过程。

甲的演算过程如下：先消去 $\exists$，得
$$\forall x (F(x) \land (G(a) \lor G(b) \lor G(c)))$$
再消去 $\forall$，得
$$\begin{aligned}
& (F(a) \land (G(a) \lor G(b) \lor G(c))) \\
& \land (F(b) \land (G(a) \lor G(b) \lor G(c))) \\
& \land (F(c) \land (G(a) \lor G(b) \lor G(c)))
\end{aligned}$$

乙的演算过程如下：先做等值演算：
$$\forall x (F(x) \land \exists y G(y)) \Leftrightarrow \forall x F(x) \land \exists y G(y)$$
再消去量词，得
$$(F(a) \land F(b) \land F(c)) \land (G(a) \lor G(b) \lor G(c))$$

显然，乙的演算过程简单些。试指出乙在演算过程中的关键步骤。

## 题 2

设个体域 $D = \{a, b, c\}$，消去下列各式中的量词。

**(1)** $\forall x \exists y (F(x) \land G(y))$
**(2)** $\forall x \forall y (F(x) \lor G(y))$
**(3)** $\forall x F(x) \to \forall y G(y)$
**(4)** $\forall x (F(x, y) \to \exists y G(y))$

## 题 3

设个体域 $D = \{1, 2\}$，请给出两种不同的解释 $I_1$ 和 $I_2$，使得下面公式在 $I_1$ 下都是真命题，而在 $I_2$ 下都是假命题。

**(1)** $\forall x (F(x) \to G(x))$
**(2)** $\exists x (F(x) \land G(x))$

## 题 4

给定公式 $A = \exists x F(x) \to \forall x F(x)$。

**(1)** 在解释 $I_1$ 中，个体域 $D = \{a\}$，证明公式 $A$ 在 $I_1$ 下的真值为 1。
**(2)** 在解释 $I_2$ 中，个体域 $D = \{a_1, a_2, \dots, a_n\}$，$n \ge 2$，$A$ 在 $I_2$ 下的真值还一定是 1 吗？为什么？

## 题 5

给定解释 $I$ 如下：

**(a)** 个体域 $D = \{3, 4\}$；
**(b)** $\bar{f}(x) : \bar{f}(3) = 4, \bar{f}(4) = 3$；
**(c)** $\bar{F}(x, y) : \bar{F}(3, 3) = \bar{F}(4, 4) = 0, \bar{F}(3, 4) = \bar{F}(4, 3) = 1$。

试求下列各式在 $I$ 下的真值。

**(1)** $\forall x \exists y F(x, y)$
**(2)** $\exists x \forall y F(x, y)$
**(3)** $\forall x \forall y (F(x, y) \to F(f(x), f(y)))$

## 题 6

甲使用量词辖域收缩与扩张等值式进行如下演算：
$$\forall x (F(x) \to G(x, y)) \Leftrightarrow \exists x F(x) \to G(x, y)$$
乙说甲错了。乙说得对吗？为什么？

## 题 7

请指出下列等值演算中的两处错误。
$$\begin{aligned}
\neg \exists x \forall y (F(x) \land (G(y) \to H(x, y))) \\
& \Leftrightarrow \forall x \exists y (F(x) \land (G(y) \to \neg H(x, y))) \\
& \Leftrightarrow \forall x \exists y ((F(x) \land G(y)) \to \neg H(x, y))
\end{aligned}$$

## 题 8

在一阶逻辑中将下列命题符号化，要求用两种不同的等值形式。

**(1)** 没有小于负数的正数。
**(2)** 相等的两个角未必都是对顶角。

## 题 9

设个体域 $D$ 为实数集合，命题“有的实数既是有理数，又是无理数”。这显然是假命题。可是某人却说这是真命题，其理由如下。设 $F(x) : x$ 是有理数，$G(x) : x$ 是无理数。$\exists x F(x)$ 与 $\exists x G(x)$ 都是真命题，因此 $\exists x F(x) \land \exists x G(x)$ 是真命题。又
$$\exists x F(x) \land \exists x G(x) \Leftrightarrow \exists x (F(x) \land G(x))$$
故 $\exists x (F(x) \land G(x))$ 也是真命题，即有的实数既是有理数，又是无理数。试问错误出在哪里。

## 题 10

$\neg \exists x (F(x) \land G(x))$ 是前束范式吗？为什么？

## 题 11

有人说无法求公式
$$\forall x (F(x) \to G(x)) \to \exists x G(x, y)$$
的前束范式，因为公式中的两个量词的指导变元相同。他的理由对吗？为什么？

## 题 12

求下列各式的前束范式。

**(1)** $\forall x F(x) \to \forall y G(x, y)$
**(2)** $\forall x (F(x, y) \to \exists y G(x, y, z))$
**(3)** $\forall x F(x, y) \leftrightarrow \exists x G(x, y)$
**(4)** $\forall x_1 (F(x_1) \to G(x_1, x_2)) \to (\exists x_2 H(x_2) \to \exists x_3 L(x_2, x_3))$
**(5)** $\exists x_1 F(x_1, x_2) \to (F(x_1) \to \neg \exists x_2 G(x_1, x_2))$

## 题 13

将下列命题符号化，要求符号化的公式为前束范式。

**(1)** 有的汽车比有的火车跑得快。
**(2)** 有的火车比所有的汽车跑得快。
**(3)** 不是所有的火车都比所有汽车跑得快。
**(4)** 有的飞机比有的汽车慢是不对的。

## 题 14

试给出实例说明，在自然推理系统 $N_{\mathcal{F}}$ 中使用 $\exists+$ 和 $\exists-$ 规则时，如果不符合规则要求的条件则可能“证明”错误的推理。

## 题 15

在自然推理系统 $N_{\mathcal{F}}$ 中，构造下列推理的证明。

**(1)**
* **前提**：$\exists x F(x) \to \forall y ((F(y) \lor G(y)) \to R(y))$，$\exists x F(x)$
* **结论**：$\exists x R(x)$

**(2)**
* **前提**：$\forall x (F(x) \to (G(a) \land R(x)))$，$\exists x F(x)$
* **结论**：$\exists x (F(x) \land R(x))$
