# 第七章 二元关系 · 习题七

## 题 1

设已知 $A = \{\varnothing, \{\varnothing\}\}$，求 $A \times P(A)$。

## 题 2

对于任意集合 $A, B, C$，若 $A \times B \subseteq A \times C$，是否一定有 $B \subseteq C$ 成立？为什么？

## 题 3

设 $A, B, C, D$ 是任意集合。

**(1)** 求证：$(A \cap B) \times (C \cap D) = (A \times C) \cap (B \times D)$。
**(2)** 下列等式哪些成立？哪些不成立？对于成立的给出证明，对于不成立的举一反例。
* $(A \cup B) \times (C \cup D) = (A \times C) \cup (B \times D)$
* $(A - B) \times (C - D) = (A \times C) - (B \times D)$

## 题 4

判断下列命题的真假，如果为真，给出证明；如果为假，给出反例。

**(1)** $A \cup (B \times C) = (A \cup B) \times (A \cup C)$
**(2)** $A \times (B \cap C) = (A \times B) \cap (A \times C)$
**(3)** 存在集合 $A$ 使得 $A \subseteq A \times A$
**(4)** $P(A) \times P(A) = P(A \times A)$

## 题 5

设 $A, B$ 为任意集合，证明：若 $A \times A = B \times B$，则 $A = B$。

## 题 6

列出从集合 $A = \{1, 2\}$ 到 $B = \{1\}$ 的所有的二元关系。

## 题 7

列出集合 $A = \{2, 3, 4\}$ 上的恒等关系 $I_A$、全域关系 $E_A$、小于等于关系 $L_A$、整除关系 $D_A$。

## 题 8

列出集合 $A = \{\varnothing, \{\varnothing\}, \{\varnothing, \{\varnothing\}\}\}$ 上的包含关系。

## 题 9

设 $A = \{1, 2, 4, 6\}$，列出下列关系 $R$。

**(1)** $R = \{\langle x, y \rangle \mid x, y \in A \land x + y \neq 2\}$
**(2)** $R = \{\langle x, y \rangle \mid x, y \in A \land |x - y| = 1\}$
**(3)** $R = \{\langle x, y \rangle \mid x, y \in A \land \frac{x}{y} \in A\}$
**(4)** $R = \{\langle x, y \rangle \mid x, y \in A \land y \text{ 为素数}\}$

## 题 10

给定 $\mathbf{Z}^+$ 上的关系 $R$ 和 $S$，$\forall x, y \in \mathbf{Z}^+$，满足
$$xRy \Leftrightarrow x \text{ 整除 } y$$
$$xSy \Leftrightarrow 5x \le y$$
对于下面每个小题，确定哪些有序对属于给定的关系：

**(1)** 关系 $R \cup S$，有序对：$\langle 2, 6 \rangle, \langle 3, 17 \rangle, \langle 2, 1 \rangle, \langle 0, 0 \rangle$；
**(2)** 关系 $R \cap S$，有序对：$\langle 3, 6 \rangle, \langle 1, 2 \rangle, \langle 2, 12 \rangle$；
**(3)** 关系 $\sim R$（以全域关系为全集），有序对：$\langle 1, 5 \rangle, \langle 2, 8 \rangle, \langle 3, 15 \rangle$。

## 题 11

$R_i$ 是 $X$ 上的二元关系，对于 $x \in X$ 定义集合
$$R_i(x) = \{y \mid x R_i y\}$$
显然 $R_i(x) \subseteq X$。如果 $X = \{-4, -3, -2, -1, 0, 1, 2, 3, 4\}$，且令
$$R_1 = \{\langle x, y \rangle \mid x, y \in X \land x < y\}$$
$$R_2 = \{\langle x, y \rangle \mid x, y \in X \land y - 1 < x < y + 2\}$$
$$R_3 = \{\langle x, y \rangle \mid x, y \in X \land x^2 \le y\}$$
求 $R_1(0), R_1(1), R_2(0), R_2(-1), R_3(-1), R_3(3)$。

## 题 12

设 $A = \{0, 1, 2, 3\}$，$R$ 是 $A$ 上的关系，且
$$R = \{\langle 0, 0 \rangle, \langle 0, 3 \rangle, \langle 2, 0 \rangle, \langle 2, 1 \rangle, \langle 2, 3 \rangle, \langle 3, 2 \rangle\}$$
给出 $R$ 的关系矩阵和关系图。

## 题 13

设
$$A = \{\langle 1, 2 \rangle, \langle 2, 4 \rangle, \langle 3, 3 \rangle\}$$
$$B = \{\langle 1, 3 \rangle, \langle 2, 4 \rangle, \langle 4, 2 \rangle\}$$
求 $A \cup B$, $A \cap B$, $\mathrm{dom} A$, $\mathrm{dom} B$, $\mathrm{dom}(A \cup B)$, $\mathrm{ran} A$, $\mathrm{ran} B$, $\mathrm{ran}(A \cap B)$, $\mathrm{fld}(A - B)$。

## 题 14

设
$$R = \{\langle 0, 1 \rangle, \langle 0, 2 \rangle, \langle 0, 3 \rangle, \langle 1, 2 \rangle, \langle 1, 3 \rangle, \langle 2, 3 \rangle\}$$
求 $R \circ R$, $R^{-1}$, $R \circ (R \circ R^{-1})$, $R \circ (R \circ R^{-1}) \circ R$。

## 题 15

设 $R = \{\langle 0, 1 \rangle, \langle 1, 2 \rangle, \langle 2, 0 \rangle\}$，求 $R^{-1}, R^2, R^3, R^4$。

## 题 16

设 $A = \{a, b, c, d\}$，$R_1, R_2$ 为 $A$ 上的关系，其中
$$R_1 = \{\langle a, a \rangle, \langle a, b \rangle, \langle b, d \rangle\}$$
$$R_2 = \{\langle a, d \rangle, \langle b, c \rangle, \langle b, d \rangle, \langle c, b \rangle\}$$
求 $R_1 \circ R_2$, $R_2 \circ R_1$, $R_1^2, R_2^2, R_1^3$。

## 题 17

设 $A = \{1, 2, 3\}$，试给出 $A$ 上两个不同的关系 $R_1$ 和 $R_2$，使得 $R_1^2 = R_1, R_2^2 = R_2$。

## 题 18

证明定理 7.4 的 (1), (2), (4)。

## 题 19

证明定理 7.5 的 (2), (3)。

## 题 20

设 $R_1$ 和 $R_2$ 为 $A$ 上的关系，证明：

**(1)** $(R_1 \cup R_2)^{-1} = R_1^{-1} \cup R_2^{-1}$
**(2)** $(R_1 \cap R_2)^{-1} = R_1^{-1} \cap R_2^{-1}$

## 题 21

设 $A = \{1, 2, \dots, 10\}$，定义 $A$ 上的关系
$$R = \{\langle x, y \rangle \mid x, y \in A \land x + y = 10\}$$
说明 $R$ 具有哪些性质并说明理由。

## 题 22

给定 $A = \{1, 2, 3, 4\}$，$A$ 上的关系 $R = \{\langle 1, 3 \rangle, \langle 1, 4 \rangle, \langle 2, 3 \rangle, \langle 2, 4 \rangle, \langle 3, 4 \rangle\}$，试：

**(1)** 画出 $R$ 的关系图；
**(2)** 说明 $R$ 的性质。

## 题 23

设 $A = \{1, 2, 3\}$。图 7.11 给出了 12 种 $A$ 上的关系，对于每种关系写出相应的关系矩阵，并说明它的性质。

*(注：图 7.11 详见原教材关系图)*

## 题 24

请看表 7.2，试对表中打“$\surd$”部分的命题给出证明，对打“$\times$”部分的命题举出反例。

## 题 25

设 $R$ 的关系图如图 7.12 所示，试给出 $r(R), s(R)$ 和 $t(R)$ 的关系图。

*(注：图 7.12 详见原教材关系图)*

## 题 26

设 $A = \{1, 2, 3, 4, 5, 6\}$，$R$ 为 $A$ 上的关系，$R$ 的关系图如图 7.13 所示。

**(1)** 求 $R^2, R^3$ 的集合表示式；
**(2)** 求 $r(R), s(R), t(R)$ 的集合表示式。

*(注：图 7.13 详见原教材关系图)*

## 题 27

证明定理 7.10 的 (2)。

## 题 28

证明定理 7.11 的 (2) 和 (3)。

## 题 29

证明定理 7.12。

## 题 30

证明定理 7.13 的 (1) 和 (3)。

## 题 31

设 $A = \{1, 2, 3, 4\}$，$R$ 是 $A$ 上的等价关系，且 $R$ 在 $A$ 上所构成的等价类是 $\{1\}$，$\{2, 3, 4\}$。

**(1)** 求 $R$；
**(2)** 求 $R \circ R^{-1}$；
**(3)** 求 $R$ 的传递闭包。

## 题 32

对于给定的 $A$ 和 $R$，判断 $R$ 是否为 $A$ 上的等价关系。

**(1)** $A$ 为实数集，$\forall x, y \in A$，$xRy \Leftrightarrow x - y = 2$
**(2)** $A = \{1, 2, 3\}$，$\forall x, y \in A$，$xRy \Leftrightarrow x + y \neq 3$
**(3)** $A = \mathbf{Z}^+$，即正整数集，$\forall x, y \in A$，$xRy \Leftrightarrow xy$ 是奇数
**(4)** $A = P(X)$，其中 $|X| \ge 2$，$\forall x, y \in A$，$xRy \Leftrightarrow x \subseteq y \lor y \subseteq x$
**(5)** $A = P(X)$，$C \subseteq X$，$\forall x, y \in A$，$xRy \Leftrightarrow x \oplus y \subseteq C$

## 题 33

设 $A = \{a, b, c, d\}$，$A$ 上的等价关系
$$R = \{\langle a, b \rangle, \langle b, a \rangle, \langle c, d \rangle, \langle d, c \rangle\} \cup I_A$$
画出 $R$ 的关系图，并求出 $A$ 中各元素的等价类。

## 题 34

设 $\pi$ 是正整数集 $\mathbf{Z}^+$ 的子集族，判断 $\pi$ 是否构成 $\mathbf{Z}^+$ 的划分。

**(1)** $S_1 = \{x \mid x \in \mathbf{Z}^+ \land x \text{ 是素数}\}$，$S_2 = \mathbf{Z}^+ - S_1$，$\pi = \{S_1, S_2\}$；
**(2)** $\pi = \{\{x\} \mid x \in \mathbf{Z}^+\}$。

## 题 35

对任意非空的集合 $A$ 且 $P(A) - \{\varnothing\}$ 是 $A$ 的非空集合族，$P(A) - \{\varnothing\}$ 是否构成 $A$ 的划分？

## 题 36

设 $A = \{1, 2, 3, 4\}$，在 $A \times A$ 上定义二元关系 $R$，
$$\forall \langle u, v \rangle, \langle x, y \rangle \in A \times A, \langle u, v \rangle R \langle x, y \rangle \Leftrightarrow u + y = x + v$$

**(1)** 证明 $R$ 是 $A \times A$ 上的等价关系；
**(2)** 确定由 $R$ 引起的对 $A \times A$ 的划分。

## 题 37

设 $A = \{a, b, c, d, e, f\}$，$R$ 是 $A$ 上的关系，且
$$R = \{\langle a, b \rangle, \langle a, c \rangle, \langle e, f \rangle\}$$
设 $R^* = tsr(R)$，则 $R^*$ 是 $A$ 上的等价关系。

**(1)** 给出 $R^*$ 的关系矩阵；
**(2)** 写出商集 $A / R^*$。

## 题 38

设 $R$ 为 $A$ 上的自反和传递的关系，证明 $R \cap R^{-1}$ 是 $A$ 上的等价关系。

## 题 39

设 $R$ 是 $A$ 上的自反关系，证明 $R$ 是 $A$ 上等价关系的充分必要条件是：若 $\langle a, b \rangle \in R$ 且 $\langle a, c \rangle \in R$，则 $\langle b, c \rangle \in R$。

## 题 40

设 $R$ 为 $\mathbf{N} \times \mathbf{N}$ 上的二元关系，$\forall \langle a, b \rangle, \langle c, d \rangle \in \mathbf{N} \times \mathbf{N}$，
$$\langle a, b \rangle R \langle c, d \rangle \Leftrightarrow b = d$$

**(1)** 证明：$R$ 为等价关系；
**(2)** 求商集 $\mathbf{N} \times \mathbf{N} / R$。

## 题 41

设 $A = \{1, 2, 3, 4\}$，$R$ 为 $A \times A$ 上的二元关系，$\forall \langle a, b \rangle, \langle c, d \rangle \in A \times A$，
$$\langle a, b \rangle R \langle c, d \rangle \Leftrightarrow a + b = c + d$$

**(1)** 证明：$R$ 为等价关系；
**(2)** 求 $R$ 导出的划分。

## 题 42

设 $R$ 是 $A$ 上的自反和传递关系，如下定义 $A$ 上的关系 $T$，证明 $T$ 是 $A$ 上的等价关系。
$$\langle x, y \rangle \in T \Leftrightarrow \langle x, y \rangle \in R \land \langle y, x \rangle \in R$$
